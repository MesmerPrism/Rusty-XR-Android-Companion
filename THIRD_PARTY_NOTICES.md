# Third-Party Notices

This file is a staging placeholder. Before publication or binary release, fill
it with dependency notices for the Android Gradle plugin, Kotlin, AndroidX,
Compose, Material, JSON/test dependencies, Gradle wrapper, and any vendored or
native code.

The copied ADB protocol implementation under `com.cgutman.adblib` requires a
separate provenance and license decision before this repo is published.

No native `liblsl` binary is bundled in the default public staging build.
